2026-07-04 22:08 · cms-admin-ui · shell: relogin switch-user respects SPA basename + ConfirmDialog; nav adds Reviews/Analytics/Catalog categories, SYSTEM order per E5; removed orphaned global-search.tsx; DataTable row actions support disabled+hint; new DateRangePicker component
2026-07-04 22:32 · cms-auth · 2FA login: 202 mfa_required → code step on the login card (TOTP/recovery, throttle), forced-enroll gate for security.mfa_required_roles, mock mfa endpoints; qrcode.react registered in E2 §2
2026-07-08 12:00 · /aif-implement · /me collection labels (C1 content) now localized to the active UI locale — mock resolves via getLocale() (COLLECTION_LABELS ×8 locales), me query keyed by locale + keepPreviousData so the sidebar follows language live (fixes English UI showing «Блог»)

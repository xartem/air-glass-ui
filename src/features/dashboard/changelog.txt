2026-07-04 23:02 · cms-dashboard · widget engine: API/mock GET /dashboard(+?role)/widgets/{key} + PUT/DELETE /layout/{roleKey} + POST /cache/clear, 17 catalog-v1 widgets, StatCard/ChartCard/ListCard/StatusCard (Recharts) on a 12-col WidgetGrid, quick actions row (confirm on cache.clear), per-role customize mode (dnd order, size tiers, hide/restore, dirty-guard, deep-link /?customize={role}), dashboard.empty state; deleted recent-activity-widget; i18n keys synced across all 8 dictionaries
2026-07-05 10:47 · /aif-implement · widget polish: meta.icon contract + shared lucide resolver (dashboard-icons), WidgetCardFrame icon chip + quiet hover, StatCard trend pill + area sparkline + tabular-nums, ChartCard gradient area/bars + dashed grid, ListCard/StatusCard state icons + row rhythm + compact empty, rigid per-archetype skeletons + error icon, richer mock fixtures (lg-tier lists, 14-pt series); specs synced (D:dashboard §4 icon, UI:dashboard §2 anatomy, E6 §3)
2026-07-05 11:35 · /aif-implement · size scale sm/md/lg/xl (25/50/75/100): WidgetSize+WIDGET_SPAN 3/6/9/12, tier branches (legend→xl, rich→lg+), CHART_HEIGHT/LIST_ROWS, customize 4-size menu, glassier card tokens, denser tiers (big sm numbers, vertical fill); specs synced (D:dashboard §4, UI:dashboard §2-3, orders/auth §6, E1 §2.1, cms-dashboard skill ×3 mirrors)
2026-07-06 00:00 · /aif-implement · widget data enrichment (A): optional archetype fields — list thumb/avatar/leadIcon + right metric column + row state accent, stat compare/goal/dated-sparkline tooltip, chart summary-header/reference-line/dashed prev-period, status clickable rows + mini-trend; 17 mocks populated + 5 new widgets (B) media.storage/mail.queue/search.queries/pages.pipeline/seo.health; specs synced (D:dashboard §4 forms, E6, admin/dashboard §2-3, media/search/mail/pages/seo §6); i18n across all 8 dictionaries; B1 attention-digest deferred (needs per-row RBAC)
2026-07-06 15:36 · /aif-implement · widget lists: localize known badge tokens (status.badge.*), drop the row box-shadow state accent (list.item.state removed), auth.activity now shows action + entity subject + entity type
2026-07-06 16:55 · /aif-implement · dashboard grid → masonry (D:dashboard §4): content-height cards, no row stretch, grid-auto-flow dense pack (WidgetGrid masonry + WidgetCardFrame self-measures grid-row span via ResizeObserver); customize grid unchanged (dnd DOM order). Spec: §4 tier=own-impl+inherit-down rule + masonry layout, admin/dashboard §2, E6
2026-07-06 18:01 · /aif-implement · bespoke widget tiers (D:dashboard §4 "иная форма" + inherit-down): stat sm/xl full-width sparkline & mini-chart, status all rows at every tier + xl table, list xl two-column; skeletons updated. Removed dead list.item.state
2026-07-06 21:46 · /aif-implement · widget row polish: single-line list row (title + right-aligned hint + rating/metric/badge) fills wide rows (auth.activity) and always shows the subject; list.item.rating → site-style amber Star icon (reviews, subject now visible); top_pages show page title + URL; search zero-result → «нет результатов» not «0». Spec §4 list row + rating
2026-07-06 21:52 · /aif-implement · RowMetric: delta moved inline (compact green/red pill next to the value) instead of stacked below — list metric rows keep uniform height (search queries, top pages, records by type)
2026-07-08 00:00 · /aif-implement · per-role quick-action layout: dashboard.layouts → nested {widgets, actions}; actions carry sort/hidden (no size). Mock getDashboard filters actions by TARGET role in customize (fixed me.permissions bug) + applies order/hidden; saveLayout validates action keys. Customize screen gains a «Quick actions» dnd+hide section, shared «Hidden» panel now covers actions; ActionLayoutOverride type; i18n actions_title/actions_all_hidden + generalized hidden_title/empty across all 8 dictionaries. Specs synced (D:dashboard §3/§4/§6/§6a/§9/§15, admin/dashboard §2/§3/§5); §14 action E2E deferred
2026-07-08 12:10 · /aif-implement · Sparkline gets a bespoke compact tooltip (content-width: series_labels date over the value) instead of the shared ChartTooltipContent whose min-w-32 + justify-between left a wide empty box around a single value; shared chart.tsx untouched (still used by ChartCard). Spec E6 §Sparkline note added
2026-07-08 12:30 · /aif-implement · global period picker (week/month/quarter, ToggleGroup in header, default month). Contract: Period type + DashboardWidgetMeta.period_aware; data(ctx, period) in mock with server-side clamp + bucketing (quarter → ~13 weekly points, NOT 90 daily; list stays ≤10; state widgets no-op). widget(key, period?) API; only period_aware widgets key on + refetch with the period. i18n dashboard.period.* ×8. Specs updated D:dashboard §4 (period contract table + overload guard) / §6 (?period=) / §13 (cache key) / §15 docs, admin/dashboard §2/§5, E6
2026-07-08 12:45 · /aif-implement · full-width sparkline: fullWidth stat sparkline (sm/xl) bleeds edge-to-edge (negative -mx card-spacing + w calc, AreaChart left/right margin → 0), card overflow-hidden clips to rounded corners; verified leftGap/rightGap = 0 vs card. Spec E6 §Sparkline note
2026-07-08 12:55 · /aif-implement · sparkline fill gradient more pronounced: vertical stops 0.25→0 replaced with 0.45 (top) / 0.15 (55%) / 0 (bottom) — denser body, was nearly invisible. Rendering detail; E6 already says «area с градиентной заливкой» (spec untouched)
